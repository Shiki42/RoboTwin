#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
RUNTIME="${ACM_RUNTIME:-/home/kye/share/parallelvla}"
exec "${RUNTIME}/bin/python" "${ROOT}/script/action_consequence.py" \
  --repo-root "${ROOT}" \
  --robotwin-root "${ROOT}" "$@"
